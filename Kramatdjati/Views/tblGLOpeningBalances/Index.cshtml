@model IEnumerable<Kramatdjati.Models.tblGLOpeningBalance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccPeriod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Posting)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccPeriod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Posting)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tblGLOpeningBalanceId }, new { @class = "btn btn-xs btn-primary" }) 
            @Html.ActionLink("Delete", "Delete", new { id = item.tblGLOpeningBalanceId }, new { @class = "btn btn-xs btn-danger" })
        </td>
    </tr>
}

</table>
