@model IEnumerable<Kramatdjati.Models.MasterRole>

@{
    ViewBag.Title = "Index";
}

<h2>App Role</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NamaRole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NamaRole)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MasterRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MasterRoleID }) |
            @Html.ActionLink( "Form", "Index", "MasterRoleDetails", new { id = item.MasterRoleID }, null) 
        </td>
    </tr>
}

</table>
